# ===========================================
# Daily News Aggregator - Environment Config
# ===========================================

# Telegram Configuration
# Create a bot via @BotFather and get your chat ID from /getUpdates
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# GitHub Configuration
# Generate a PAT with 'repo' scope at: Settings -> Developer settings -> Personal access tokens
GITHUB_TOKEN=ghp_your_token_here
GITHUB_REPO=username/daily-news-collections
GITHUB_BRANCH=main

# CCS Configuration
# Available models: sonnet, opus, haiku (or full model IDs like claude-3-5-sonnet-latest)
# Note: When using CCS GLM profile, the model is handled by CCS
CLAUDE_MODEL=sonnet

# CCS Profile (default: glm for GLM LLM)
# Options: glm, claude, gemini, codex, etc. (run 'ccs' to see all available profiles)
CCS_PROFILE=glm

# News Preset
# Available presets: ai_tech, china_tech, global_scan, finance, dev_community
# You can also define custom presets in config/presets.json
NEWS_PRESET=ai_tech

# Git User (for commits)
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your@email.com
